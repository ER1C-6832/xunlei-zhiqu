# Public deployment setting only. Never put a model/API key in the extension.
# Local Runtime default:
VITE_RUNTIME_URL=http://127.0.0.1:8765

# For a later remote Runtime/Gateway build, override with its HTTPS origin, e.g.
# VITE_RUNTIME_URL=https://runtime.example.com
